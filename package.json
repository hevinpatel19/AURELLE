{
  "name": "aurelle-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && CI=false npm run build",
    "install-all": "cd client && npm install && cd ../server && npm install"
  }
}
